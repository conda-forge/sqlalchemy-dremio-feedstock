{% set name = "sqlalchemy-dremio" %}
{% set version = "3.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sqlalchemy_dremio-{{ version }}.tar.gz
  sha256: cb559597e6821e85fc2dcd764b77e9b2d9701f1c1b916400a5e88a0009237d63

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - pyarrow
    - pyodbc >=4.0.17
    - python >=3.6
    - sqlalchemy >=1.2

test:
  imports:
    - sqlalchemy_dremio
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/narendrans/sqlalchemy_dremio
  summary: A SQLAlchemy dialect for Dremio via the ODBC and Flight interface.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - cshaley
